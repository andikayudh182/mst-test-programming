const user = require('./m_user')
const barang = require('./m_barang')
const customer = require('./m_customer')
const sales = require('./t_sales')
const sales_detail = require('./sales_detail')

module.exports = {
    user,
    barang,
    customer,
    sales,
    sales_detail
}